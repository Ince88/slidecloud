[build]
  publish = "."

[functions]
  node_bundler = "esbuild"

# Viewer: /s/<slug>
[[redirects]]
  from   = "/s/*"
  to     = "/index.html"
  status = 200

# Presenter remote: /present/<slug>?token=...
[[redirects]]
  from   = "/present/*"
  to     = "/index.html"
  status = 200
